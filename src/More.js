function More(){
    return (
        <div>
        <h2>this is testing page so we check this page run or note when we render inside of index pages</h2>

        </div>

    );
}

export default More();